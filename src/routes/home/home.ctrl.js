"use strict";

const db = require("../../models/db");
const table = require("../../models/UserStorage");

const GET = {
    각자 맡은 파트: (req, res) => {

    },
};

const POST = {
    각자 맡은 파트: (req, res) => {

    },
};

module.exports = {
    GET,
    POST,
};